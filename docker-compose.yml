version: "3"
services:
  app:
    build: .
    ports:
      - "4879:4879"
    environment:
      - SENG365_MYSQL_HOST=db
      - SENG365_MYSQL_PORT=3306
    depends_on:
      - db
  db:
    image: "mysql:5.7"
    environment:
      - MYSQL_ROOT_PASSWORD=secret
